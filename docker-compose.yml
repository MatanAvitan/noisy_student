version: '2'

services:
  noisy_student:
    build: .
    container_name: noisystudent
    image: bestteam/noisystudent:latest
    restart: always
    volumes:
      - data:/noisy_student/src/data-mscoco
volumes:
  data:
